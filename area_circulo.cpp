#include <bits/stdc++.h>

using namespace std;

int main(){
    auto pi =  3.14159;
    int R; cin>>R;

    cout<<fixed<<setprecision(4);
    cout<<"A="<<pi*(R*R);
    //cout<<"A="<<pi*pow(R,2);

    return 0;
}